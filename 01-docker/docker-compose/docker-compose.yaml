version: "3.8"

services:
  image-name-service:
    image: <aws-account-id>.dkr.ecr.us-<region>.amazonaws.com/<repository-name>:image-name-service
    container_name: image-name-service
    restart: always
    env_file:
      - /home/ubuntu/.config/service-envs/image-name-service/.env
    volumes:
      - /home/ubuntu/.config/mount-file/keyToMount:/usr/src/app/keyToMount
    ports:
      - "<host-port>:<container-port>"
    networks:
      - <network-name>
    deploy:
      resources:
        limits:
          memory: 1g
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "2"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:<container-port>/v1/health"]
      interval: 60s
      timeout: 30s
      retries: 3
      start_period: 10s

networks:
  <network-name>:
    external: true